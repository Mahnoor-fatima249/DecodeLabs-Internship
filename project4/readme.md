# Object Detection using MobileNet-SSD
This project uses Python and OpenCV to detect objects in images using a pre-trained Caffe model.

## Files:
- `main.py`: The main Python script.
- `MobileNetSSD_deploy.prototxt`: The model architecture.
- `MobileNetSSD_deploy.caffemodel`: The pre-trained weights.

